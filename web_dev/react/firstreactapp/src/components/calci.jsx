function add(x, y) {
  return x + y;
}
function subtact(x, y) {
  return x - y;
}
function multiply(x, y) {
  return x * y;
}
function divide(x, y) {
  return x / y;
}

export { add, subtact, multiply, divide };
